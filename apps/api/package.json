{
  "name": "@crashtest/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node --import tsx src/server.ts",
    "test": "vitest run",
    "lint": "echo 'lint skipped'"
  },
  "dependencies": {
    "@crashtest/engine": "workspace:*",
    "@crashtest/openmetadata-client": "workspace:*",
    "@crashtest/shared": "workspace:*",
    "fastify": "^4.28.1",
    "@fastify/cors": "^9.0.1",
    "zod": "^3.23.8",
    "tsx": "^4.19.1"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1",
    "supertest": "^7.0.0"
  }
}
